<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.poria.upms.mapper.SysDeptMapper">
	<select id="listDepts" resultType="cn.poria.upms.api.entity.SysDept">
		SELECT * FROM sys_dept WHERE del_flag = '0'
	</select>

	<select id="getOrgByDeptId" resultType="cn.poria.upms.api.entity.SysDept">
		WITH RECURSIVE cte AS (
			SELECT *
			FROM sys_dept
			WHERE dept_id = #{deptId}
			UNION ALL
			SELECT n.*
			FROM sys_dept n
			INNER JOIN cte ON n.dept_id = cte.parent_id
		)
		SELECT * FROM cte WHERE type = 1;
	</select>
</mapper>
