server:
  port: 5002

spring:
  application:
    name: @artifactId@
  profiles:
    active: @profiles.active@
springdoc:
  api-docs:
    enabled: ${SWAGGER_ENABLE:true}
#定时任务
xxl:
  job:
    accessToken: default_token
    admin:
      addresses: http://${XXL_HOST:poria-xxl}:${XXL_PORT:9080}/xxl-job-admin
    executor:
      appname: xxl-job-executor-sample
      address:
      ip:
      port: 9999
      logpath: /data/applogs/xxl-job/jobhandler
      logretentiondays: 30
