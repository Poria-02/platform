# 页面标题
VITE_APP_TITLE = Poria 管理台

# 生产环境配置
VITE_APP_ENV = 'staging'

# 预发布环境沿用 Poria 的网关前缀；部署时覆盖实际网关地址。
VITE_APP_BASE_API = '/api'
VITE_AUTH_CLIENT_ID = ''
VITE_AUTH_SCOPE = 'server'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
